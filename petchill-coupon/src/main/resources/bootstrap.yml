spring:
  application:
    name: petchill-coupon
  cloud:
    nacos:
      config:
        server-addr: localhost:8848 #Nacos作为配置中心地址（本机的写localhost）
        namespace: 11b83049-82af-4c3b-b691-b02699a16235
        group: dev
        ext-config[0]:
          data-id: datasource.yml
          group: dev
          refresh: true
        ext-config[1]:
          data-id: mybatis.yml
          group: dev
          refresh: true
        ext-config[2]:
          data-id: other.yml
          group: dev
          refresh: true